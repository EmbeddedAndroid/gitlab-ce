- @hide_top_links = true
- page_title "Foundries"
- header_title  "Foundries", dashboard_groups_path
= render 'dashboard/groups_head'

= webpack_bundle_tag 'common_vue'
= webpack_bundle_tag 'groups'

- if @groups.empty?
  = render 'empty_state'
- else
  = render 'groups'
